function copierTexte_1() {
    const p = document.getElementById('texteACopier_1');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}


function copierTexte_2() {
    const p = document.getElementById('texteACopier_2');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_3() {
    const p = document.getElementById('texteACopier_3');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_4() {
    const p = document.getElementById('texteACopier_4');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}






function copierTexte_5() {
    const p = document.getElementById('texteACopier_5');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}


function copierTexte_6() {
    const p = document.getElementById('texteACopier_6');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_7() {
    const p = document.getElementById('texteACopier_7');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_8() {
    const p = document.getElementById('texteACopier_8');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}



function copierTexte_9() {
    const p = document.getElementById('texteACopier_9');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}


function copierTexte_10() {
    const p = document.getElementById('texteACopier_10');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_11() {
    const p = document.getElementById('texteACopier_11');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_12() {
    const p = document.getElementById('texteACopier_12');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_13() {
    const p = document.getElementById('texteACopier_13');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}

function copierTexte_14() {
    const p = document.getElementById('texteACopier_14');
    const selection = window.getSelection();
    const range = document.createRange();
    range.selectNode(p);
    selection.removeAllRanges();
    selection.addRange(range);
    document.execCommand('copy');
    selection.removeAllRanges(); // Désélectionner le texte
}